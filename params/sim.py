from brian2 import *

break_time = 0*ms#300 * ms   # Time for the network to settle (break stage)
stim_time  = 100*ms#0 * ms  # Time for the network to be stimulated (stimulus stage)
# break_time = 300 * ms   # Time for the network to settle (break stage)
# stim_time  = 1000 * ms  # Time for the network to be stimulated (stimulus stage)